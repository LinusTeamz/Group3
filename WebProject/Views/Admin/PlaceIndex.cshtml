@model IEnumerable<WebProject.Models.Place>

@{
    ViewBag.Title = "PlaceIndex";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h2>PlaceIndex</h2>

<p>
    @Html.ActionLink("Create New", "PlaceCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        <td>
            @Html.ActionLink("Edit", "PlaceEdit", new { id=item.Id }) |
            @Html.ActionLink("Details", "PlaceDetails", new { id=item.Id }) |
            @Html.ActionLink("Delete", "PlaceDelete", new { id=item.Id })
        </td>
    </tr>
}

</table>
